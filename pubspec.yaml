name: index3
description: 端末内のファイルをデータ形式ごとに分類して「整理整頓」フォルダへまとめてコピーするAndroidアプリ。
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  # ストレージアクセス権限の要求
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
